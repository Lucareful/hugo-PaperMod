name: Build Check

on:
  push:
    paths-ignore:
      - "images/**"
      - "LICENSE"
      - "README.md"
    branches:
      - master
      - exampleSite
  pull_request:
    paths-ignore:
      - "images/**"
      - "LICENSE"
      - "README.md"
    branches:
      - master
      - exampleSite
  workflow_dispatch:
    # manual run
    inputs:
      hugoVersion:
        description: "Hugo Version"
        required: false
        default: "0.146.0"

defaults:
  run:
    shell: bash

jobs:
  build-check:
    runs-on: ubuntu-latest
    env:
      # min_version from theme.toml
      HUGO_VERSION: ${{ github.event.inputs.hugoVersion || '0.146.0' }}
    steps:
      - name: Install Hugo CLI
        run: |
          wget -O ${{ runner.temp }}/hugo.deb https://github.com/gohugoio/hugo/releases/download/v${HUGO_VERSION}/hugo_${HUGO_VERSION}_linux-amd64.deb \
          && sudo dpkg -i ${{ runner.temp }}/hugo.deb

      - name: Checkout example site
        uses: actions/checkout@v6
        with:
          repository: adityatelange/hugo-PaperMod
          ref: exampleSite
          path: exampleSite

      - name: Remove example site theme submodule placeholder
        run: rm -rf exampleSite/themes/hugo-PaperMod

      - name: Checkout current theme
        uses: actions/checkout@v6
        with:
          path: exampleSite/themes/hugo-PaperMod

      - name: Build with Hugo
        working-directory: exampleSite
        run: hugo --buildDrafts --gc
